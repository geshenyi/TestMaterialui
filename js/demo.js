/**
 * @jsx React.DOM
 */

var React = require('react'),
    mui = require('material-ui'),
    PaperButton = mui.PaperButton;

var button = <PaperButton label="Demo"/>;

React.render(button, document.body);
